#include <iostream>
#include <vector>
#include <string>

// A complex function that might look like obfuscation to a human, but is valid C++
void complex_logic() {
    std::string s = "Hello";
    for(int i=0; i<100; i++) {
        s += std::to_string(i);
    }
    std::cout << s.length() << std::endl;
}

int main() {
    // This code is > 300 characters (add comments to pad it out)
    // ........................................................
    // ........................................................
    // ........................................................
    complex_logic();
    return 0;
}